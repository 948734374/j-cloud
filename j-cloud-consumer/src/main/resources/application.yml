# 应用端口
server:
  port: 8001
spring:
  application:
    # 应用名称
    name: Consumer

# eureka 配置
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

logging:
  level:
    root: INFO